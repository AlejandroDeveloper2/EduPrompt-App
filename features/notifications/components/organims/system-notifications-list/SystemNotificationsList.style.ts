import { SizeType } from "@/core/types";

import { NotificationListStyle } from "../notification-list/NotificationList.style";

export const SystemNotificationListStyle = (size: SizeType) =>
  NotificationListStyle(size);
